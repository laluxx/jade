use raylib
use GLFW.glfw3

fn sub(x:i32, y:i32) -> i32 {
    return x - y;
}

fn voidFunction() {

}

let x:f32 = 29;

#define COMPTIME 10;

type Vec3i {
    x:f32;
    y:f32;
    z:i32;
}

let a = 29;
let b = 29.0;
let s = "text";
let c = 'a';

fn main() -> i32 {
    let variable:i32 = sub(2, 2); // NOTE This works somehow
    let x:i32 = 10;
    defer x = 0;
    
    if x > 3 {
        printf("Hello World\n");
    }


    loop {
        println!("something");
        break;
    }

    
    sub(3, -1); // ANOTHER
    // Comment
    println!("Hello!");
    return 0;
}

fn add(x:i32, y:i32) -> i32 {
    return x + y;
}

